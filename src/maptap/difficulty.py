from .countries import Country


def score_country_difficulty(country: Country) -> float:
    """Score a country's difficulty.

    Args:
        country: The query country

    Returns:
        float: The difficulty score.
    """
    ...